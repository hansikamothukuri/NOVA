# Database Configuration (MySQL)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=nova_db

# Security & Authentication
JWT_SECRET=super_secret_jwt_key_change_in_production
JWT_EXPIRES_IN=7d

# Server Configuration
PORT=3000
NODE_ENV=development

# Client Configuration
CLIENT_URL=
VITE_API_URL=/api

# Optional: Server-side Gemini API (if AI assistance features are enabled)
GEMINI_API_KEY=
APP_URL=

